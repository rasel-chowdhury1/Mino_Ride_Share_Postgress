// ── Mongoose model removed — data layer is now Prisma (PostgreSQL) ──────────
// All queries are in user.service.ts via prisma client.
// This file is intentionally empty to preserve the import path for legacy
// references while the full migration is in progress.

export {};
